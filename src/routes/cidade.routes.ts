import { Router } from 'express';
// import CidadeController from '../controllers/cidade.controller';

const router = Router();
// const cidadesController = new CidadesController();

// router.get('/cidades', cidadesController.read.bind(cidadesController));
// router.get('/cidades/:id', cidadesController.readOne.bind(cidadesController));

export default router;